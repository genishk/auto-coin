name: ETH Signal Check (4H) - Detailed Action

on:
  # 4시간마다 자동 실행 (UTC 기준)
  schedule:
    - cron: '15 0,4,8,12,16,20 * * *'  # 매 4시간봉 마감 15분 후
  
  # 수동 실행 가능 (테스트용)
  workflow_dispatch:

jobs:
  check-signals:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run detailed signal check
      id: signal_check
      run: python scripts/check_eth_4h_detailed.py
    
    - name: Send email notification
      if: steps.signal_check.outputs.signal_type == 'buy' || steps.signal_check.outputs.signal_type == 'sell'
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.EMAIL_USERNAME }}
        password: ${{ secrets.EMAIL_PASSWORD }}
        subject: |
          💎 [ETH 4H] ${{ steps.signal_check.outputs.signal_type == 'buy' && '🟢 매수 시그널' || '🔴 매도 시그널' }} (${{ steps.signal_check.outputs.current_time }})
        to: ${{ secrets.EMAIL_TO }}
        from: Auto-Coin ETH Bot <${{ secrets.EMAIL_USERNAME }}>
        body: |
          ═══════════════════════════════════════════════════════════
          💎 Auto-Coin ETH 4시간봉 [상세 알림]
          ═══════════════════════════════════════════════════════════

          📅 시간: ${{ steps.signal_check.outputs.current_time }} (UTC)
          💰 현재가: ${{ steps.signal_check.outputs.current_price }}
          📊 RSI: ${{ steps.signal_check.outputs.current_rsi }}

          ═══════════════════════════════════════════════════════════
          📍 현재 포지션 상태
          ═══════════════════════════════════════════════════════════
          
          🔹 롱 포지션: ${{ steps.signal_check.outputs.has_position == 'yes' && '보유 중' || '없음' }}
          🔹 물타기: ${{ steps.signal_check.outputs.water_count }}회
          🔹 미실현 손익: ${{ steps.signal_check.outputs.unrealized }}%
          🔹 숏 헷징: ${{ steps.signal_check.outputs.hedge_status }}

          ═══════════════════════════════════════════════════════════
          🚨 시그널 & 액션
          ═══════════════════════════════════════════════════════════

          ${{ steps.signal_check.outputs.signal_type == 'buy' && '🟢 매수 시그널 발생!' || '🔴 매도 시그널 발생!' }}

          📋 액션: ${{ steps.signal_check.outputs.actions }}

          ═══════════════════════════════════════════════════════════
          
          📌 ETH 전략: RSI 35/40 → 85/55, 헷징 50%
          💡 자세한 내용은 대시보드에서 확인하세요.

